// ---------------------------------------------------------

import Component from './component'
import fixtures from './fixtures'

// ---------------------------------------------------------

const Jumbotron = (props) => <Component {...props} />

export default Jumbotron

// ---------------------------------------------------------

export { Component as component, fixtures }
